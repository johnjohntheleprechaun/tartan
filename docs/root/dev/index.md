# Tartan Developer Docs

Currently, the only written part of Tartan is the context tree loader. To fuck with that, you'll need to fuck with `rxjs`.

Here's some basic stuff about the project structure of Tartan:

First of all, this repo is divided into two packages, `cli` and `core`. I might decide to change that later, idk.
The `core` package is documented [here](./packages/core), and `cli` is documented [here](./packages/cli)
